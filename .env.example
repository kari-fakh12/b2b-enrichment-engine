# Copy to .env and fill in. .env is gitignored, never commit it.

# People and company search, mobile status and reveal
PROSPEO_API_KEY=
# Phone fallback for people Prospeo has no number for
BETTERCONTACT_API_KEY=
# Read-only HubSpot private app token, for CRM dedupe
HUBSPOT_TOKEN=
# Works-here check on LinkedIn profiles
APIFY_TOKEN=
